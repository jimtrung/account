function manualLogin() {
  const username = document.querySelector('.username-input').value;
  const password = document.querySelector('.password-input').value;

  // Send to backend

  // Success then redirect

}

function googleLogin() {
  // Redirect to login shit
}